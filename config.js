var config = {
    host: 'dev2.paalgyula.com', // Hostname
    port: 25565, // Port
    username: 'Minion', // Login name
    password: '********' // Password (for /login command)
};

module.exports = config;